version: '3'

services:
    homework:
        image: nginx:latest
        ports:
            - "80:80"
        volumes: 
            - ./nginx.conf:/etc/nginx/conf.d/nginx.conf
            - .:/Homework
    php:
        image: php:fpm
        volumes:
            - .:/Homework