/**
 * Создайте скрипт, который запрашивает ввод двух чисел (используйте prompt) и после показывает их сумму.
 */
let num1 = +prompt("Num1 is ", 0);
let num2 = +prompt("Num2 is ", 0);
alert(Number(num1) + Number(num2));